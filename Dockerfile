# Используем официальный образ Java 11
FROM openjdk:11

# Установка рабочей директории внутри контейнера
WORKDIR /usr/src/app

# Копируем скомпилированные классы в контейнер
COPY out/ /usr/src/app

# Запускаем приложение при старте контейнера
CMD ["java", "-cp", "/usr/src/app", "ru.Main"]

